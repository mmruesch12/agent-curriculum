{
  "name": "agent-architect-academy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-bundle.js",
    "test": "node --test tests/*.test.js",
    "serve": "npx --yes serve web -p 3456",
    "verify:launch": "node scripts/verify-launch.js",
    "verify:all": "npm run build && npm test && node scripts/verify-static.js && node scripts/verify-sim-export.js && npm run verify:launch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.61.1"
  }
}